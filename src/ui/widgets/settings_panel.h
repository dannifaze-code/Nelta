#ifndef SETTINGS_PANEL_H\n#define SETTINGS_PANEL_H\n\n#include <QWidget>\n\nclass SettingsPanel : public QWidget {\n    Q_OBJECT\n\npublic:  // Constructor and Destructor\n    SettingsPanel(QWidget *parent = nullptr);\n    ~SettingsPanel();\n\nprivate:  // UI Elements\n    void createTabs();\n    void setupVideoTab();\n    void setupAudioTab();\n    void setupControlsTab();\n    void setupSkinsTab();\n\nprivate slots:  // Slot functions for tab changes\n    void onTabChanged(int index);\n};\n\n#endif // SETTINGS_PANEL_H